@model HovisMillingPortal.Models.ApplicationUser

@using HovisMillingPortal.Helpers
@using WebGrease.Css.Extensions

<header class="header">
    <h3>Delete User</h3>
</header>
<div class="content-wrap">
    <div class="row">

        <div class="col-md-12">

            <section class="panel">
                <div class="panel-body ">

                    <p>Do you really want to delete user @Model.Email </p>

                    @using (Html.BeginForm("Delete", "ApplicationUser", FormMethod.Post))
                    {
                        @Html.HiddenFor(x => x.Id);
                        <a href="@Url.Action("IndexList")" class="btn btn-success">No</a>
                        <input type="submit" class="btn btn-danger" value="Yes" />
                    }
                </div>
            </section>
        </div>
    </div>
</div>